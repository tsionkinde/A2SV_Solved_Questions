from collections import Counter
class Solution:
    def singleNumber(self, nums: List[int]) -> int:
        result=0
        count=Counter(nums)
        for num in nums:
            if count[num]==1:
                result=num
        return result        

        
